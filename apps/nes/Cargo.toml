[package]
name = "nes-guest"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-only"

[dependencies]
polkavm-derive = "0.31.0"

[build-dependencies]
cc = "1"

[profile.release]
opt-level = "s"
lto = true
