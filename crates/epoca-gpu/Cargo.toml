[package]
name = "epoca-gpu"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
publish = false

[features]
default = []
pvm = ["dep:polkavm-derive"]

[dependencies]
epoca-gpu-wire = { path = "../gpu-wire" }
polkavm-derive = { version = "=0.31.0", optional = true }
